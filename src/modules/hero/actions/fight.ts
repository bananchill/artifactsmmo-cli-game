import { Effect } from "effect";
// import { requestPost } from "../../../common/http/index.js";

// export const Fight = (heroName: string) =>
//   Effect.gen(function* () {
//     const result = yield* requestPost(, { participants: [] });
//     return result;
//   });
